/**
 * Example runtime configuration file.
 * Copy to public/config.js and set apiBaseUrl for your environment.
 * The real config.js is git-ignored and never committed.
 *
 * Authentication is handled by the in-app login screen — no token here.
 */
window.__APP_CONFIG__ = {
  apiBaseUrl: "/api/v1",

  // Optional: public URL of the OpenMetadata catalog UI. When set, a "Data Catalog"
  // link appears in the sidebar and opens OM in a new tab. Leave unset if not deployed.
  // catalogUrl: "http://localhost:8585",
};
