apiVersion: v1
kind: Secret
metadata:
  name: {{ include "novasight.secretName" . }}
  labels:
    {{- include "novasight.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := (.Values.secrets | default dict) }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
