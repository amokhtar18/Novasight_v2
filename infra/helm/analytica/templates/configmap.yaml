apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "analytica.configMapName" . }}
  labels:
    {{- include "analytica.labels" . | nindent 4 }}
data:
  {{- /* All values rendered as strings; ConfigMap data must be string-valued. */}}
  {{- $config := include "analytica.effectiveConfig" . | fromYaml }}
  {{- range $k, $v := $config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
