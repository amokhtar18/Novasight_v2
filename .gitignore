# Secrets — never commit a real .env (placeholders live in .env.example)
.env
.env.*
!.env.example

# Superpowers local scratch (session ports/tokens — never commit)
.superpowers/brainstorm/

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
